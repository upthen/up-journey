# ===== up-journey 环境变量（本地与 NAS 各一份，绝不提交真实口令）=====

# NAS 共享相册宿主机绝对路径（唯一图片数据源，只读挂载；本地开发指向本机某个测试目录）
PHOTOS_HOST_DIR=/volume1/家庭相册

# MySQL（生产为 NAS docker volume，不暴露端口）
MYSQL_ROOT_PASSWORD=change-me-root
MYSQL_DATABASE=upjourney
MYSQL_USER=upjourney
MYSQL_PASSWORD=change-me-upjourney
