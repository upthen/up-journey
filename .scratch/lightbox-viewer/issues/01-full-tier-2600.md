# 01: 高清档提清——full 档 1600→2600，缓存键纳入尺寸

**What to build:** 访客放大照片到约 130% 时仍然清晰。后端高清档默认尺寸提到 ~2600px；缓存键纳入尺寸数值，使配置变更后旧缓存自然失效、按需重建，无需人工清理。

**Blocked by:** None (can start immediately)

**Status:** resolved

- [x] 高清档默认尺寸配置为 2600（部署环境样例同步）
- [x] 缓存键包含尺寸数值：改配置后旧缓存不命中，重新生成
- [x] API 表面测试覆盖：档位上限取配置值、缓存键随尺寸配置变化
- [x] 现有全部测试通过

## Comments

2026-09-06 实现：`full_size` 默认 2600，缓存键纳入尺寸数值；新增两条 API 测试（档位上限、配置变更缓存失效），后端 100 测试全绿。
