# 01: 高清档提清——full 档 1600→2600，缓存键纳入尺寸

**What to build:** 访客放大照片到约 130% 时仍然清晰。后端高清档默认尺寸提到 ~2600px；缓存键纳入尺寸数值，使配置变更后旧缓存自然失效、按需重建，无需人工清理。

**Blocked by:** None (can start immediately)

**Status:** ready-for-agent

- [ ] 高清档默认尺寸配置为 2600（部署环境样例同步）
- [ ] 缓存键包含尺寸数值：改配置后旧缓存不命中，重新生成
- [ ] API 表面测试覆盖：档位上限取配置值、缓存键随尺寸配置变化
- [ ] 现有全部测试通过
